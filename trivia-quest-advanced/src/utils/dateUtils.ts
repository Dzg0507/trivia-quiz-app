export const getISOWeek = (date) => {  const d = new Date(date);  d.setHours(0, 0, 0, 0);  d.setDate(d.getDate() + 3 - (d.getDay() + 6) % 7);  const week1 = new Date(d.getFullYear(), 0, 4);  return 1 + Math.round(((d - week1) / 86400000 + (week1.getDay() + 6) % 7 - 3) / 7);};export const getTodayIdentifier = () => new Date().toISOString().split('T')[0];export const getThisWeekIdentifier = () => {  const date = new Date();  const year = date.getFullYear();  const week = getISOWeek(date);  return `${year}-W${week < 10 ? '0' + week : week}`;};export const getThisMonthIdentifier = () => new Date().toLocaleDateString('en-US', { month: 'long', year: 'numeric' });